name: generative-journey
dependencies:
  - python=3.10
  - pip
  - jupyterlab
  - ipykernel
  - matplotlib
  - tqdm
  - pip:
    - torch
    - scikit-learn
    - imageio

# Set environment variables
variables:
  KMP_DUPLICATE_LIB_OK: TRUE

